version: '3.8'

services:
 web:
  build:
   context: .
  ports:
  - 8001:5001
  volumes:
  - ./:/app

 mysqldb:
  image: mysql
  ports:
  - 3307:3307
  environment:
  - MYSQL_ROOT_PASSWORD=p@ssw0rd1
  volumes:
  - mysql:/var/lib/mysql
  - mysql_config:/etc/mysql

volumes:
  mysql:
  mysql_config: